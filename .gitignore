# BAT scripts
*.bat

# Pester test result report - generated by Run-Tests.ps1, not source
TestResults.xml

# pre-push-lint-and-tests skill config (per-host, declares the local gate set)
.pre-push.json

# graphify knowledge graph artifacts (local; rebuilt by graphify)
graphify-out/
supersets-out/
